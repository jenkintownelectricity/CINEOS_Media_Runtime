"""
CINEOS Media Runtime — HTTP API Bridge

Exposes the Python media backend to the TypeScript UI layer via HTTP.
All modules use Python stdlib only (no external dependencies).
"""

__version__ = "0.1.0"
